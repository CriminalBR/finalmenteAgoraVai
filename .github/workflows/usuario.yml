name: usuarios do push

on:
  push:
    branches: [main]
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      -  name: Checkout code
         uses: actions/checkout@v3

      -  name: pegando usuario do push
         run: echo "Usuário do GitHub: ${{ github.actor }}"
